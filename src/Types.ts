export type ProjectType = "node" | "gradle" | "maven" | "composer" | "pip";

export enum ProjectTypes {
    Node = "node",
    Gradle = "gradle",
    Maven = "maven",
    Composer = "composer",
    Pip = "pip",
}